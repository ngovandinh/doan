
                <div class="col-md-3">
                    <!-- *** MENUS AND FILTERS ***
 _________________________________________________________ -->
                    <div class="panel panel-default sidebar-menu">

                        <div class="panel-heading">
                            <h3 class="panel-title">Bài Biết mới nhất</h3>
                        </div>

                        <div class="panel-body">
                            <ul class="nav nav-pills nav-stacked category-menu">
                                <% Category.all.each do |category|  %>
                                <li>
                                    <a href="category.html"><%= category.name  %><span class="badge pull-right"></span></a>
                                    <% category.lessions.limit(10).order(created_at: :desc).each do |lession|  %>
                                    <ul>
                                        <li><%= link_to lession.name, customer_lession_path(lession) %></a></li>
                                    </ul>
                                    <% end %>
                                </li>
                                <% end %>
                            </ul>

                        </div>
                    </div>
            </div>
